# Bluff backend

The Convex backend contains Bluff's schema, functions, scheduling, settlement, and file
storage.

Run `pnpm exec convex dev` from the project root while developing. Files in
`_generated` are generated by Convex and should not be edited by hand. See the
[project README](../README.md) for setup and environment configuration.
